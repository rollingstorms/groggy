[package]
name = "python-groggy"
version = "0.3.0"
edition = "2021"

[lib]
name = "groggy"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
groggy = { path = "../" }  # Path to the main Rust library
serde_json = "1.0"
